export default class {
    constructor() {

    }

    // for changing the title name in the browser tab
    setTitle(title) {
        document.title = title;
    }

    async getHtml() {
        return "";
    }
}